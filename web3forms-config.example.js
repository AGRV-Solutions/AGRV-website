/**
 * Optional: you can hand-edit web3forms-config.js, OR use `.env` + sync script:
 *
 *   cp .env.example .env
 *   # edit .env — set WEB3FORMS_ACCESS_KEY=your-uuid
 *   node scripts/sync-env-to-web3forms.js
 *
 * `web3forms-config.js` is gitignored (generated or hand-made).
 * GitHub Pages: use repo secret WEB3FORMS_ACCESS_KEY + Actions workflow (readme.md).
 */
window.AGRV_WEB3FORMS_KEY = '';
